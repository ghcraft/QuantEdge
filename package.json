{
  "name": "marketlivefeed",
  "version": "1.0.2",
  "private": "true",
  "scripts": {
    "dev": "next dev",
    "dev:server": "node server.js",
    "build": "prisma generate && node scripts/fix-prisma-client.js && next build",
    "build:production": "prisma generate && prisma migrate deploy && next build",
    "build:local": "prisma generate && next build",
    "start": "next start",
    "lint": "next lint",
    "postinstall": "prisma generate && node scripts/fix-prisma-client.js",
    "vercel-build": "prisma generate && node scripts/fix-prisma-client.js && next build",
    "migrate": "prisma migrate deploy"
    "prisma:generate": "prisma generate",
    "migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^7.0.1",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^20.11.0",
    "@types/node-cron": "^3.0.11",
    "autoprefixer": "^10.4.0",
    "bcryptjs": "^3.0.3",
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.0",
    "jsonwebtoken": "^9.0.2",
    "next": "^14.2.0",
    "node-cron": "^3.0.3",
    "postcss": "^8.4.0",
    "prisma": "^7.0.1",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "recharts": "^3.5.0",
    "rss-parser": "^3.13.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.3.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0"
  }
}
